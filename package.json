{
  "name": "crossbill",
  "version": "0.27.1",
  "description": "Crossbill - KOReader highlights sync",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "prepare": "node -e \"if (process.env.CI !== 'true') try { require('child_process').execSync('husky', { stdio: 'inherit' }) } catch {}\"",
    "lint": "npm run lint --workspace=frontend",
    "format": "npm run format --workspace=frontend"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.2.0",
    "typescript": "~6.0.3"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx}": [
      "bash -c 'cd frontend && eslint --fix \"$0\"'",
      "bash -c 'cd frontend && prettier --write \"$0\"'"
    ],
    "frontend/**/*.{json,md,css}": [
      "bash -c 'cd frontend && prettier --write \"$0\"'"
    ],
    "backend/**/*.py": [
      "bash -c 'cd backend && uv run ruff format \"$0\"'",
      "bash -c 'cd backend && uv run ruff check --fix \"$0\"'",
      "bash -c 'cd backend && uv run pyright \"$0\"'"
    ]
  }
}
